version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew assembleDebug
      - ls
      - ls android 
      - ls android/app/build/outputs/
artifacts:
  files:
     - './android/app/build/outputs/**/*.apk'
  discard-paths: yes
